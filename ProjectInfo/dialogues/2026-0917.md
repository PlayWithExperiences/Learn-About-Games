# 2026-09-17 原始对话索引

## 0732 NotebookLM 每日生产

决策：無涘（自动化任务既有授权） ｜ 记录：Codex

用户原始指令与实际工具回合见本场 Codex transcript，session 01a0ac90-2785-7440-9095-4b17ba2ca942；本文件不声称已导出完整 transcript。

用户任务：执行项目共享 collect-resources-about-game skill；每天最多 10 次 distinct claim、串行 1、自动重试 0；每条必须四项内容与资产通过校验，单文件交付 AI-Life-Mentor main 并回读精确 blob；失败和配额分别留痕，不触发消费者。

实际答复：preflight ready_to_claim，尝试 1、ready 0、远端交付 0、failed 1、跳过 9、剩余 9。运行器超时报未发现信息图，事后 Studio 已显示三张卡片，根因未确认；停止领取并保留失败，不自动重跑。未验证的卡片不能计为交付。

> —— 0804 续 ——

> 【AI】本轮停止在读取不一致：尝试 1 条、failed 1 条，ready 和远端交付均为 0，今日剩余 9 次。三张卡片虽已可见，但尚未完成内容、展开和文件验收，不能算成功；目前也没有配额耗尽证据。我会保留错误与复查记录，不自动重跑该条。

> —— 0804 续 ——

> 【AI】本轮未交付资源。
>
> - 预检：`ready_to_claim`，浏览器可用。
> - 尝试 **1**；ready **0**；远端交付 **0**；failed **1**；跳过 **9**；今日剩余 **9**。
> - 停止原因：运行器超时报未发现信息图，但复查已见三张卡片，读取差异原因尚未确认。未自动重跑，也未确认配额耗尽。
> - 未触发 Daily Check-in。记录已推送，远端 SHA 已核对。
>
> ::inbox-item{title="NotebookLM 生产因读取差异停止" summary="先排查卡片观测不一致；本轮零交付，剩余九次"}
>
> <oai-mem-citation>
> <citation_entries>
> MEMORY.md:711-719|note=[prior browser repair context checked against current implementation]
> </citation_entries>
> <rollout_ids>
> 01a09f5e-d8d1-7f02-92bc-4a8d0dc4db06
> </rollout_ids>
> </oai-mem-citation>

## 1030 收集端：先修卡片观测再恢复生产（跨日，09-17 10:21 → 09-18 13:30）

决策：無涘（既有每日最多 10 次 distinct claim、串行 1、自动重试 0 授权；另在本次明确授权两条 failed 的恢复方式） ｜ 记录：DSH agent ｜ session 8ae5270c-73a8-4a98-8abb-e9d3d5443982

用户任务（原始指令）：执行项目共享 collect-resources-about-game skill。上一场（0732 Codex）停在「卡片观测不一致」，要求先定位差异再恢复生产。

用户在本场的三次明确决定（原话摘要）：
1. 询问失败条目 `youtube-0zfm5_YqzYw` 如何恢复 → 选「只重生成信息图 + 补导出发布（不新增 claim）」。
2. 我随后更正：producer 的 publish 只接受 generating/ready，failed 必须先走 retry，无法“不新增 claim”补发 → 用户改选「用 retry 开票 + 只重做信息图（推荐）」。
3. `youtube-mnIsv2ps31U` 因隔离脚本缺陷在 isolate-source 失败后询问 → 用户选「retry 重跑这一条（推荐）」。

实际答复与产出：
- 定位到两个独立缺陷：①生成中的卡片是 `sync 正在生成信息图… 基于 1 个来源`（类型名在标题里、图标是 `sync`），旧 `wait_for_card` 先比图标再判「正在生成」，导致自己那条产物的 generating 信号永远不可达；②Studio 列表会返回与服务器脱节的快照，也会返回空数组（面板没在渲染列表），旧逻辑只在列表为空时重载一次，其余时间读到的是冻结快照。
- 修复：`classify_cards()` 先按类型词识别；列表指纹 240s 不变或读到空列表即强制 `--reload` 重载（超时前再刷新一次）；`studio_cards()` 读不到/面板未渲染时抛 `studio-read`，claim 前严格读一次，读不到就不领取；隔离脚本支持按精确 video id 匹配未解析的 URL 来源卡；生成脚本不再复制一份硬编码 8 轮的隔离循环，改为调用独立隔离脚本。回归：`tests/lib/notebooklm-card-wait.test.ts`（6 例）、`tests/lib/notebooklm-isolate-match.test.ts`（4 例）。
- 交付 2 条并远端逐字节核对：`youtube-NW4b6gP9_VY`（珠峰 VR → `2026-0917-1304-game-feel-feedback.json`）、`youtube-0zfm5_YqzWw`（独立游戏共享空间，恢复 → `2026-0917-1328-game-feel-feedback.json`）。
- 内容验收拦下 2 处：第一条信息图写了来源没有的照片张数（30 万张），重生成后通过；恢复条目的总结把房东 16,000 美元统称押金并给反馈效果加了加强词，重问一次总结后通过。两条的验收证据在各 item 目录 `content-review.json`。
- 另交付通道一次预置修复：AI-Life-Mentor `main` 与 origin 各有 1 个独立提交（`.claude/trace-health.json` 与机器人的 `pkm-index.json`，零文件重叠），合并后推送并核对两个 blob。
- `youtube-mnIsv2ps31U` 第一条失败已按授权 retry，来源复用、三条产物提交后待收尾。

::inbox-item{title="收集端卡片观测两缺陷已修并交付" summary="修 `wait_for_card` 与隔离匹配；交付 2 条、恢复 1 条、重跑 1 条"}

> —— 09-18 1330 续 ——

用户原话（三问三答）：①`youtube-0zfm5_YqzYw` 恢复方式 → 「只重生成信息图 + 补导出发布」，我随后更正 producer 状态机限制后改选「用 retry 开票 + 只重做信息图」；②`youtube-mnIsv2ps31U` 因隔离缺陷失败 → 「retry 重跑这一条」；③两条 failed 是否现在恢复 → 「两条都恢复」。

【AI】09-18 下午续做与答复：又定位并修掉两个真缺陷——(a) `nblm-capture-deck.cjs` 只取第一个下载事件，拿到 128×128 PNG 缩略图而不是 PPTX（改成优先取 `.pptx` 文件名的事件，实测取回 17,206,190 B 合法 PPTX）；(b) 隔离用的来源名取自导入时刻，元数据解析后卡片名已变，导致 `youtube-lnnsDi7Sxq0` 在 isolate-source 白花一次 claim（新增 `resolve_isolate_name()` 在隔离前用当前列表重算，并加 `--isolate-name` 测试入口）。另把今早 Codex 停批的 `title_matches` 误匹配改为「token 重合 + 字符相似度 ≥0.75」。已交付 `youtube-mnIsv2ps31U`（Stugan，`2026-0918-1423-narrative-expression.json`，远端 blob `88d30b62…`）；`youtube-lnnsDi7Sxq0` 恢复运行中。

::inbox-item{title="收集端四处缺陷已修，交付 3 条" summary="卡片观测/来源身份/隔离取名/deck 捕捉；内容验收各拦下 1 处并重生成"}

> —— 09-18 1540 续 ——

【AI】`youtube-lnnsDi7Sxq0`（Ultima Online）恢复：修掉的两个缺陷都验证后跑通，三件产物齐备且内容验收通过（含定向复核第一轮的两个假否定），但 21,883,508 B 的演示文稿被 PicGo 的 GitHub contents API 以 400 拒绝（同日 17.2/20.55 MB 的 deck 可上传），故按 `failed(upload)` 留痕、本批停止上传。已交付 3 条（珠峰 VR、独立游戏共享空间、Stugan）；导图栅格化空白那一次已换图，并给 finish 脚本加了 `--reuse-artifacts`，下次只需补上传与交付。
