interface:
  display_name: "NotebookLM Daily Resource (compatibility alias)"
  short_description: "Use the shared game-resource collection skill"
  default_prompt: "Load $collect-resources-about-game for the canonical cross-client collection workflow."
policy:
  allow_implicit_invocation: false
